from django.apps import AppConfig

class VoiceCommandsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.voice_commands'
    verbose_name = 'apps.voice_commands'
